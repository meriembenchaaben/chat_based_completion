# --- Core notebook / utilities ---
jupyterlab>=4.0
ipykernel>=6.25
python-dotenv>=1.0.0
tqdm>=4.66.0

# --- Hugging Face download (custom cache dir supported) ---
huggingface_hub[cli]>=0.23.0

# --- OpenAI-compatible client (works with vLLM server) ---
openai>=1.30.0
httpx>=0.27.0

# --- Optional: data handling / analysis later ---
numpy>=1.26.0
pandas>=2.2.0
pyyaml>=6.0.1

# --- Optional: JSON schema validation for model outputs ---
jsonschema>=4.22.0
pydantic>=2.7.0

# --- Serving (GPU) ---
vllm>=0.7.0
